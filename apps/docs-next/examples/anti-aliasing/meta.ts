import type { ExampleMetaDefinition } from '../../lib/example-meta';
export const meta = { slug: 'anti-aliasing', title: 'Anti-Aliasing', description: 'One high-contrast scene through Off, MSAA 4×, SSAA 2×, and FXAA — pick a mode and watch the edges.', tags: ['anti-aliasing', 'msaa', 'ssaa', 'fxaa'], capabilities: ['webgpu', 'render-targets', 'select-control', 'multi-pass', 'responsive-canvas'], thumb: { warmupFrames: 60, dt: 1 / 60, time: 1.2 }, files: ['index.tsx', 'controls.tsx', 'types.ts', 'renderer.ts', 'scene.wgsl', 'resolve.wgsl', 'fxaa.wgsl'] } as const satisfies ExampleMetaDefinition;
