---
"@vgpu/core": minor
"@vgpu/adapter-node": minor
---

Add explicit `cubeView()` and `layerView()` texture view helpers plus `Device.isCompatibilityMode` plumbing for compatibility-sensitive cubemap shaders.
