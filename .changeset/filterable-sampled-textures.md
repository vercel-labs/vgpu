---
"@vgpu/wgsl": patch
"vgpu": patch
---

Infer filterable sampled-float texture layouts per entry point while preserving unfilterable load-only textures, and report actionable errors for unsupported float32 formats.
