# by-example §4 shared uniforms

Runnable main API (`vgpu`) example for by-example §4 shared uniforms. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init(canvas)` and `gpu.screen`.
