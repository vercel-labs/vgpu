# by-example §8 ping-pong

Runnable ring-1 example for by-example §8 ping-pong. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init(canvas)` and `gpu.screen`.
