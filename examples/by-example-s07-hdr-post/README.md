# by-example §7 targets and post

Runnable main API (`vgpu`) example for by-example §7 targets and post. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init() + gpu.surface(canvas)` and `surface`.
