# by-example §7 targets and post

Runnable ring-1 example for by-example §7 targets and post. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init(canvas)` and `gpu.screen`.
