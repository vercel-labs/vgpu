# by-example §6 scene draw

Runnable ring-1 example for by-example §6 scene draw. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init(canvas)` and `gpu.screen`.
