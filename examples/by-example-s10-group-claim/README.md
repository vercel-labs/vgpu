# by-example §10 group claim

Runnable ring-1 example for by-example §10 group claim. The Vitest file uses `vgpu/node` so it can run in the Docker GPU harness; browser code is the same except for `init(canvas)` and `gpu.screen`.
