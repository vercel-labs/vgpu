{
  "name": "vgpu-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=22 <23" },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:docker": "docker build --platform linux/arm64 -f infra/test-docker/Dockerfile .",
    "bundle-check": "node scripts/check-bundle-size.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
