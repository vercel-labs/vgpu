{
  "name": "vgpu-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.15.4",
  "engines": { "node": ">=22 <23" },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "test:fast": "vitest run packages/adapter-mock packages/core",
    "test:docker": "docker buildx build --platform linux/arm64 -t vgpu-test:s1 -f infra/test-docker/Dockerfile . && docker run --rm vgpu-test:s1 sh -lc 'Xvfb :99 -screen 0 1024x768x24 >/tmp/xvfb.log 2>&1 & xvfb_pid=$!; pnpm test; status=$?; kill $xvfb_pid; exit $status'",
    "bundle-check": "node scripts/check-bundle-size.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "@webgpu/types": "^0.1.69",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
