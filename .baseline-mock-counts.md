# Baseline mock pipeline/module creation counts

Captured before Task 02 code changes in `/home/user/vgpu-worktrees/async-dx` on branch `feat/async-dx`.

## Command

```sh
pnpm exec vitest run packages/vgpu-api/tests/.tmp-baseline-mock-counts.test.ts --reporter=verbose
```

Temporary test scenario: two `gpu.draw()` instances with byte-identical WGSL draw once to the same mock target. `createShaderModule` was measured with a Vitest spy because baseline mock instrumentation does not expose a shader-module counter yet; render pipelines used `getMockGPUDeviceInstrumentation`.

## Counts

| Scenario | createShaderModule | createRenderPipeline | createRenderPipelineAsync |
| --- | ---: | ---: | ---: |
| Two identical draws, same target/signature | 2 | 2 | 0 |

## Baseline result

```text
stdout | packages/vgpu-api/tests/.tmp-baseline-mock-counts.test.ts > baseline duplicate draw module and pipeline creation counts
{"createShaderModule":2,"createRenderPipeline":2,"createRenderPipelineAsync":0}

✓ packages/vgpu-api/tests/.tmp-baseline-mock-counts.test.ts (1)
```

Full pre-change package test note: `pnpm exec vitest run packages/vgpu-api/tests` had all runtime tests green but one unrelated existing tooling fixture failure:

```text
FAIL  packages/vgpu-api/tests/tooling/typing-fixture.test.ts > published vgpu/client types resolve WebGPU globals without monorepo tsconfig
Error: TypeScript reported diagnostics:
node_modules/vgpu/client.d.ts:8:34 - error TS2307: Cannot find module '@vgpu/wgsl/loader-vite' or its corresponding type declarations.
node_modules/vgpu/client.d.ts:9:62 - error TS2307: Cannot find module '@vgpu/wgsl/loader-vite' or its corresponding type declarations.
```
