# Pinned toolchain for validate-cpu.py (Python 3.11).
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# Validation only. The conversion toolchain moved to requirements-convert.txt:
# TensorFlow 2.15.1 and tf2onnx 1.16.1 both cap dependencies that a current,
# CVE-patched onnx requires, so one file could not carry both. The exact
# resolver conflicts are recorded in requirements-convert.txt.
onnx==1.22.0
onnxruntime==1.20.1
numpy==1.26.4
pillow==11.3.0
