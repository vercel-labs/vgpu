export const docsManifest = {
  "schemaVersion": 1,
  "generatedFrom": "docs/allowlist.txt",
  "records": [
    {
      "package": "@vgpu/adapter-mock",
      "symbol": "createMockAdapter",
      "repoPath": "packages/adapter-mock/src/createMockAdapter.docs.md",
      "virtualPath": "/@vgpu/adapter-mock/createMockAdapter.docs.md",
      "content": "# createMockAdapter\n\n`createMockAdapter()` returns a pure-JavaScript `VGPUAdapter`. Its devices allocate\nin-memory buffers backed by `Uint8Array`, making core testable without Dawn or native\nGPU libraries.\n"
    },
    {
      "package": "@vgpu/adapter-node",
      "symbol": "createNodeAdapter",
      "repoPath": "packages/adapter-node/src/createNodeAdapter.docs.md",
      "virtualPath": "/@vgpu/adapter-node/createNodeAdapter.docs.md",
      "content": "# createNodeAdapter\n\n`createNodeAdapter()` returns a Dawn-backed `VGPUAdapter` using `webgpu@0.4.0`.\nThat version is pinned because upstream added `dist/linux-arm64.dawn.node`, which\nlets this adapter use the official Dawn prebuilt binary. On Linux the adapter uses\nthe OpenGL software backend by default (`backend=opengl` plus Dawn compatibility\nfeature level) and keeps a process-wide singleton GPU instance; conflicting later\nflags are warned and ignored because Dawn re-init can SIGSEGV.\n\nOn Linux, the `webgpu@0.4.0` Dawn linux-arm64 prebuild may require GLIBC 2.38+\nwhen the native binary is loaded. Debian 12/bookworm (GLIBC 2.36) and similar\nhosts can fail with `/lib/aarch64-linux-gnu/libc.so.6: version 'GLIBC_2.38' not\nfound`. This is a native environment compatibility issue, not a shader or\nrendering issue. Use the pinned Docker test environment (`pnpm test:docker`) or\nanother Node 22 host based on Debian trixie, Ubuntu 24.04+, or equivalent GLIBC\n2.38+ runtime.\n\nFor headless/software rendering, match the Docker environment variables\n`LIBGL_ALWAYS_SOFTWARE=1`, `DISPLAY=:99`, and\n`XDG_RUNTIME_DIR=/tmp/xdg-runtime`. `VGPU_DAWN_FLAGS` may be set to\nspace-separated Dawn flags, such as `VGPU_DAWN_FLAGS=backend=opengl`, and\nreplaces the adapter's default backend flag selection.\n"
    },
    {
      "package": "@vgpu/adapter-node",
      "symbol": "createNodeDevice",
      "repoPath": "packages/adapter-node/src/createNodeDevice.docs.md",
      "virtualPath": "/@vgpu/adapter-node/createNodeDevice.docs.md",
      "content": "# createNodeDevice\n\n`createNodeDevice(opts?)` is a mechanical convenience that requests a `Device` from\nthe Node adapter directly. Prefer `App.create({ adapter: createNodeAdapter() })`\nwhen bootstrapping application code.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "AppCreateOptions",
      "repoPath": "packages/core/src/App.docs.md",
      "virtualPath": "/@vgpu/core/App.docs.md",
      "content": "# App\n\n`App.create({ adapter })` is the bootstrap entry point for creating a vgpu device.\nIt asks the supplied `VGPUAdapter` for a `Device` and returns the explicit aggregate\n`{ device, queue }`. It never uses ambient context or `AsyncLocalStorage`.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "AppInstance",
      "repoPath": "packages/core/src/App.docs.md",
      "virtualPath": "/@vgpu/core/App.docs.md",
      "content": "# App\n\n`App.create({ adapter })` is the bootstrap entry point for creating a vgpu device.\nIt asks the supplied `VGPUAdapter` for a `Device` and returns the explicit aggregate\n`{ device, queue }`. It never uses ambient context or `AsyncLocalStorage`.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "App",
      "repoPath": "packages/core/src/App.docs.md",
      "virtualPath": "/@vgpu/core/App.docs.md",
      "content": "# App\n\n`App.create({ adapter })` is the bootstrap entry point for creating a vgpu device.\nIt asks the supplied `VGPUAdapter` for a `Device` and returns the explicit aggregate\n`{ device, queue }`. It never uses ambient context or `AsyncLocalStorage`.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "BufferOptions",
      "repoPath": "packages/core/src/Buffer.docs.md",
      "virtualPath": "/@vgpu/core/Buffer.docs.md",
      "content": "# Buffer\n\n`Buffer` is an opaque wrapper over `GPUBuffer`. Use `write(data, offset?)` to send\nCPU bytes through the device queue and `read(byteLength, offset?)` to copy bytes\nback through the core readback ring. `.gpu` exposes the underlying `GPUBuffer` for\nmechanical WebGPU calls.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "Buffer",
      "repoPath": "packages/core/src/Buffer.docs.md",
      "virtualPath": "/@vgpu/core/Buffer.docs.md",
      "content": "# Buffer\n\n`Buffer` is an opaque wrapper over `GPUBuffer`. Use `write(data, offset?)` to send\nCPU bytes through the device queue and `read(byteLength, offset?)` to copy bytes\nback through the core readback ring. `.gpu` exposes the underlying `GPUBuffer` for\nmechanical WebGPU calls.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "BufferUsageName",
      "repoPath": "packages/core/src/Buffer.docs.md",
      "virtualPath": "/@vgpu/core/Buffer.docs.md",
      "content": "# Buffer\n\n`Buffer` is an opaque wrapper over `GPUBuffer`. Use `write(data, offset?)` to send\nCPU bytes through the device queue and `read(byteLength, offset?)` to copy bytes\nback through the core readback ring. `.gpu` exposes the underlying `GPUBuffer` for\nmechanical WebGPU calls.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "BufferWriteData",
      "repoPath": "packages/core/src/Buffer.docs.md",
      "virtualPath": "/@vgpu/core/Buffer.docs.md",
      "content": "# Buffer\n\n`Buffer` is an opaque wrapper over `GPUBuffer`. Use `write(data, offset?)` to send\nCPU bytes through the device queue and `read(byteLength, offset?)` to copy bytes\nback through the core readback ring. `.gpu` exposes the underlying `GPUBuffer` for\nmechanical WebGPU calls.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "CreateDeviceOptions",
      "repoPath": "packages/core/src/Device.docs.md",
      "virtualPath": "/@vgpu/core/Device.docs.md",
      "content": "# Device\n\n`Device` is the opaque core module around a raw `GPUDevice`. It creates buffers,\nowns the queue wrapper, captures structured validation errors through error scopes,\nand exposes the raw WebGPU object via `.gpu` for mechanical escape-hatch use.\nUse `destroy()` or `dispose()` for teardown.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "createMockGPUDevice",
      "repoPath": "packages/core/src/createMockGPUDevice.docs.md",
      "virtualPath": "/@vgpu/core/createMockGPUDevice.docs.md",
      "content": "# createMockGPUDevice\n\n`createMockGPUDevice()` creates the in-memory WebGPU-shaped object used by the mock\nadapter. It is public so `@vgpu/adapter-mock` can expose the same adapter contract as\nother adapters while keeping mock storage logic local to core tests.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "Device",
      "repoPath": "packages/core/src/Device.docs.md",
      "virtualPath": "/@vgpu/core/Device.docs.md",
      "content": "# Device\n\n`Device` is the opaque core module around a raw `GPUDevice`. It creates buffers,\nowns the queue wrapper, captures structured validation errors through error scopes,\nand exposes the raw WebGPU object via `.gpu` for mechanical escape-hatch use.\nUse `destroy()` or `dispose()` for teardown.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "Queue",
      "repoPath": "packages/core/src/Queue.docs.md",
      "virtualPath": "/@vgpu/core/Queue.docs.md",
      "content": "# Queue\n\n`Queue` wraps `GPUQueue` with the operations core needs: writing buffer data and\nwaiting for submitted work. `.gpu` exposes the raw queue for callers that need direct\nWebGPU control.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "ShaderInput",
      "repoPath": "packages/core/src/Shader.docs.md",
      "virtualPath": "/@vgpu/core/Shader.docs.md",
      "content": "# Shader\n\n`Shader` is the core opaque shader object created by `device.createShader(...)`.\nIt accepts either a raw WGSL string or a `ResolvedShader` from `@vgpu/wgsl`, then\nowns the resulting `GPUShaderModule` while keeping WGSL metadata available\nthrough a stable object.\n\nPublic shape:\n\n- `.kind` is `\"wgsl\"` for WGSL shaders.\n- `.gpu` exposes the raw `GPUShaderModule` escape hatch.\n- `.source`, `.entryPoints`, and `.stats` forward read-only metadata from the\n  resolved WGSL object.\n- `ShaderInput` is either `string` or `ResolvedShader`.\n\nInvariants: creating a shader from a string still goes through the WGSL\ncompiler path internally, but no runtime resolver is loaded. `dispose()` is a\nno-op because WebGPU shader modules have no destroy method.\n\nExample:\n\n```ts\nconst shader = device.createShader(\"@compute @workgroup_size(1) fn main() {}\");\nconsole.log(shader.kind); // \"wgsl\"\n```\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "Shader",
      "repoPath": "packages/core/src/Shader.docs.md",
      "virtualPath": "/@vgpu/core/Shader.docs.md",
      "content": "# Shader\n\n`Shader` is the core opaque shader object created by `device.createShader(...)`.\nIt accepts either a raw WGSL string or a `ResolvedShader` from `@vgpu/wgsl`, then\nowns the resulting `GPUShaderModule` while keeping WGSL metadata available\nthrough a stable object.\n\nPublic shape:\n\n- `.kind` is `\"wgsl\"` for WGSL shaders.\n- `.gpu` exposes the raw `GPUShaderModule` escape hatch.\n- `.source`, `.entryPoints`, and `.stats` forward read-only metadata from the\n  resolved WGSL object.\n- `ShaderInput` is either `string` or `ResolvedShader`.\n\nInvariants: creating a shader from a string still goes through the WGSL\ncompiler path internally, but no runtime resolver is loaded. `dispose()` is a\nno-op because WebGPU shader modules have no destroy method.\n\nExample:\n\n```ts\nconst shader = device.createShader(\"@compute @workgroup_size(1) fn main() {}\");\nconsole.log(shader.kind); // \"wgsl\"\n```\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "TextureOptions",
      "repoPath": "packages/core/src/Texture.docs.md",
      "virtualPath": "/@vgpu/core/Texture.docs.md",
      "content": "# Texture\n\n`Texture` is the core opaque GPU texture object created by\n`device.createTexture(...)`. It wraps a `GPUTexture`, tracks the creation\noptions, and provides deterministic `rgba8unorm` readback for snapshot tests.\n\n`TextureOptions` contains:\n\n- `size`: `[width, height, depthOrArrayLayers?]`.\n- `format`: currently readback-supported for `\"rgba8unorm\"` and `\"rgba8unorm-srgb\"`.\n- `usage`: texture usage names such as `\"render_attachment\"` and `\"copy_src\"`.\n- `label`: optional WebGPU label.\n\nInvariants: `read()` only supports `rgba8unorm` and throws structured\n`VGPU-CORE-UNSUPPORTED-FORMAT` for unsupported formats. `createView(...)`\nforwards to the raw texture. `destroy()` is idempotent; after destroy, `read()`\nthrows because the texture is no longer alive.\n\nExample:\n\n```ts\nconst target = device.createTexture({\n  size: [256, 256],\n  format: \"rgba8unorm\",\n  usage: [\"render_attachment\", \"copy_src\"],\n});\nconst pixels = await target.read();\n```\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "Texture",
      "repoPath": "packages/core/src/Texture.docs.md",
      "virtualPath": "/@vgpu/core/Texture.docs.md",
      "content": "# Texture\n\n`Texture` is the core opaque GPU texture object created by\n`device.createTexture(...)`. It wraps a `GPUTexture`, tracks the creation\noptions, and provides deterministic `rgba8unorm` readback for snapshot tests.\n\n`TextureOptions` contains:\n\n- `size`: `[width, height, depthOrArrayLayers?]`.\n- `format`: currently readback-supported for `\"rgba8unorm\"` and `\"rgba8unorm-srgb\"`.\n- `usage`: texture usage names such as `\"render_attachment\"` and `\"copy_src\"`.\n- `label`: optional WebGPU label.\n\nInvariants: `read()` only supports `rgba8unorm` and throws structured\n`VGPU-CORE-UNSUPPORTED-FORMAT` for unsupported formats. `createView(...)`\nforwards to the raw texture. `destroy()` is idempotent; after destroy, `read()`\nthrows because the texture is no longer alive.\n\nExample:\n\n```ts\nconst target = device.createTexture({\n  size: [256, 256],\n  format: \"rgba8unorm\",\n  usage: [\"render_attachment\", \"copy_src\"],\n});\nconst pixels = await target.read();\n```\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "TextureUsageName",
      "repoPath": "packages/core/src/Texture.docs.md",
      "virtualPath": "/@vgpu/core/Texture.docs.md",
      "content": "# Texture\n\n`Texture` is the core opaque GPU texture object created by\n`device.createTexture(...)`. It wraps a `GPUTexture`, tracks the creation\noptions, and provides deterministic `rgba8unorm` readback for snapshot tests.\n\n`TextureOptions` contains:\n\n- `size`: `[width, height, depthOrArrayLayers?]`.\n- `format`: currently readback-supported for `\"rgba8unorm\"` and `\"rgba8unorm-srgb\"`.\n- `usage`: texture usage names such as `\"render_attachment\"` and `\"copy_src\"`.\n- `label`: optional WebGPU label.\n\nInvariants: `read()` only supports `rgba8unorm` and throws structured\n`VGPU-CORE-UNSUPPORTED-FORMAT` for unsupported formats. `createView(...)`\nforwards to the raw texture. `destroy()` is idempotent; after destroy, `read()`\nthrows because the texture is no longer alive.\n\nExample:\n\n```ts\nconst target = device.createTexture({\n  size: [256, 256],\n  format: \"rgba8unorm\",\n  usage: [\"render_attachment\", \"copy_src\"],\n});\nconst pixels = await target.read();\n```\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "ValidationError",
      "repoPath": "packages/core/src/VGPUError.docs.md",
      "virtualPath": "/@vgpu/core/VGPUError.docs.md",
      "content": "# VGPUError\n\n`VGPUError` is the structured error base class. Every core error has a stable\n`code`, human message, optional `fix`, and optional `where` field so agents can\nhandle failures as data instead of parsing prose.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "VGPUAdapter",
      "repoPath": "packages/core/src/VGPUAdapter.docs.md",
      "virtualPath": "/@vgpu/core/VGPUAdapter.docs.md",
      "content": "# VGPUAdapter\n\n`VGPUAdapter` is the interface implemented by concrete adapters. Core asks an adapter\nfor a `Device` and does not know whether the implementation is Dawn-backed,\nbrowser-backed, or pure JavaScript mock memory.\n"
    },
    {
      "package": "@vgpu/core",
      "symbol": "VGPUError",
      "repoPath": "packages/core/src/VGPUError.docs.md",
      "virtualPath": "/@vgpu/core/VGPUError.docs.md",
      "content": "# VGPUError\n\n`VGPUError` is the structured error base class. Every core error has a stable\n`code`, human message, optional `fix`, and optional `where` field so agents can\nhandle failures as data instead of parsing prose.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "InspectMaterial",
      "repoPath": "packages/render/src/inspect/inspect-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/inspect-material.docs.md",
      "content": "# InspectMaterial\n\n`InspectMaterial` is the low-level material shape used by the inspect sub-export.\nIt contains a pipeline, a group-0 bind group layout, and a typed uniform writer.\n\n```ts\nconst uniforms = device.createBuffer({\n  size: material.uniformByteSize,\n  usage: [\"uniform\", \"copy_dst\"],\n});\n\nmaterial.writeUniforms(uniforms.gpu, 0, {\n  viewProjectionMatrix: camera.viewProjectionMatrix,\n  modelMatrix,\n});\n```\n\n`writeUniforms` writes the shared inspect matrices into the buffer. This keeps\ninspect materials independent from application materials used by `RapidRenderer`.\n\n`InspectMaterialUniformParams` includes `viewProjectionMatrix` and `modelMatrix`.\nFuture inspect materials should extend this interface explicitly if they need\nadditional uniform inputs.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "InspectMaterialUniformParams",
      "repoPath": "packages/render/src/inspect/inspect-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/inspect-material.docs.md",
      "content": "# InspectMaterial\n\n`InspectMaterial` is the low-level material shape used by the inspect sub-export.\nIt contains a pipeline, a group-0 bind group layout, and a typed uniform writer.\n\n```ts\nconst uniforms = device.createBuffer({\n  size: material.uniformByteSize,\n  usage: [\"uniform\", \"copy_dst\"],\n});\n\nmaterial.writeUniforms(uniforms.gpu, 0, {\n  viewProjectionMatrix: camera.viewProjectionMatrix,\n  modelMatrix,\n});\n```\n\n`writeUniforms` writes the shared inspect matrices into the buffer. This keeps\ninspect materials independent from application materials used by `RapidRenderer`.\n\n`InspectMaterialUniformParams` includes `viewProjectionMatrix` and `modelMatrix`.\nFuture inspect materials should extend this interface explicitly if they need\nadditional uniform inputs.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "meshToWireframe",
      "repoPath": "packages/render/src/inspect/mesh-to-wireframe.docs.md",
      "virtualPath": "/@vgpu/render/inspect/mesh-to-wireframe.docs.md",
      "content": "# meshToWireframe\n\n`meshToWireframe(mesh, device)` converts a readable triangle-list `Mesh` into a\nline-list index buffer with duplicate edges removed by endpoint position.\n\n```ts\nimport { meshToWireframe } from \"@vgpu/render/inspect\";\n\nconst wireframe = await meshToWireframe(readableMesh, device);\n\n// wireframe.vertexBuffer is readableMesh.vertexBuffer\n// wireframe.indexBuffer contains two indices per line segment\n```\n\nThe source mesh's vertex buffer must be created with `GPUBufferUsage.COPY_SRC`\nso `meshToWireframe` can read back vertex positions. If the buffer is not\nreadable, the function throws `VGPU-CORE-INVALID-USAGE`; `Mesh.box` currently\ndoes not satisfy this contract.\n\nEndpoints are quantized to a `1e-6` grid before comparison. Meshes with distinct\nfeatures below that scale may have edges incorrectly merged, so author debug\ngeometry at a larger scale or use a precomputed wireframe for tiny details.\n\nCoplanar triangle diagonals are omitted by comparing triangle face normals, so a\nreadable unit-box triangle list produces 12 line segments and 24 indices.\n\nThe returned `indexBuffer` is a raw `GPUBuffer`. Callers own its lifetime and\nshould call `indexBuffer.destroy()` when done, or rely on `device.destroy()` to\nclean up at teardown.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "normalDebugMaterial",
      "repoPath": "packages/render/src/inspect/normal-debug-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/normal-debug-material.docs.md",
      "content": "# normalDebugMaterial\n\nRenders each fragment colored by its surface normal, which is useful for\nverifying mesh normals visually during development.\n\n```ts\nimport { Mesh } from \"@vgpu/render\";\nimport { normalDebugMaterial } from \"@vgpu/render/inspect\";\n\nconst mesh = Mesh.box({ device });\nconst material = normalDebugMaterial({ device });\n```\n\nEach fragment outputs `(normal + 1) / 2` as RGB, mapping the unit-cube normal\nrange `[-1, +1]³` to `[0, 1]³`. A face whose normal points along +X reads pure\nred; +Y reads pure green; +Z reads pure blue. Reverse directions read cyan,\nmagenta, and yellow.\n\nThe pipeline uses `triangle-list` topology with depth testing and back-face\nculling. Default `targetFormat` is `'bgra8unorm-srgb'`; use\n`'rgba8unorm-srgb'` on platforms that require it.\n\nNormals are passed through in object space. Applying a rotation in `modelMatrix`\nrotates the visualized colors with the geometry, which is the intended behavior\nfor this inspector. A normal matrix is not applied; this is a development\nvisualization, not a lighting pass.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "NormalDebugMaterialSpec",
      "repoPath": "packages/render/src/inspect/normal-debug-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/normal-debug-material.docs.md",
      "content": "# normalDebugMaterial\n\nRenders each fragment colored by its surface normal, which is useful for\nverifying mesh normals visually during development.\n\n```ts\nimport { Mesh } from \"@vgpu/render\";\nimport { normalDebugMaterial } from \"@vgpu/render/inspect\";\n\nconst mesh = Mesh.box({ device });\nconst material = normalDebugMaterial({ device });\n```\n\nEach fragment outputs `(normal + 1) / 2` as RGB, mapping the unit-cube normal\nrange `[-1, +1]³` to `[0, 1]³`. A face whose normal points along +X reads pure\nred; +Y reads pure green; +Z reads pure blue. Reverse directions read cyan,\nmagenta, and yellow.\n\nThe pipeline uses `triangle-list` topology with depth testing and back-face\nculling. Default `targetFormat` is `'bgra8unorm-srgb'`; use\n`'rgba8unorm-srgb'` on platforms that require it.\n\nNormals are passed through in object space. Applying a rotation in `modelMatrix`\nrotates the visualized colors with the geometry, which is the intended behavior\nfor this inspector. A normal matrix is not applied; this is a development\nvisualization, not a lighting pass.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "wireframeMaterial",
      "repoPath": "packages/render/src/inspect/wireframe-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/wireframe-material.docs.md",
      "content": "# wireframeMaterial\n\n`wireframeMaterial` renders a mesh as line segments, which is useful for\nvisualizing geometry edges during development.\n\n```ts\nimport { meshToWireframe, wireframeMaterial } from \"@vgpu/render/inspect\";\n\nconst wireframe = await meshToWireframe(readableMesh, device);\nconst material = wireframeMaterial({ device, color: [1, 1, 1] });\n```\n\nThe pipeline uses `line-list` topology with depth testing on and no back-face\nculling. It expects the same interleaved position and normal vertex layout as the\nsource mesh passed to `meshToWireframe`.\n\n`color` defaults to `[1, 1, 1]` in linear RGB. `targetFormat` defaults to\n`'bgra8unorm-srgb'`; use `'rgba8unorm-srgb'` on platforms that require it.\n\nThe returned `InspectMaterial` is for low-level draws. Pair it with\n`meshToWireframe`, bind a uniform buffer at group 0, then issue an indexed\nline-list draw.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "WireframeMaterialSpec",
      "repoPath": "packages/render/src/inspect/wireframe-material.docs.md",
      "virtualPath": "/@vgpu/render/inspect/wireframe-material.docs.md",
      "content": "# wireframeMaterial\n\n`wireframeMaterial` renders a mesh as line segments, which is useful for\nvisualizing geometry edges during development.\n\n```ts\nimport { meshToWireframe, wireframeMaterial } from \"@vgpu/render/inspect\";\n\nconst wireframe = await meshToWireframe(readableMesh, device);\nconst material = wireframeMaterial({ device, color: [1, 1, 1] });\n```\n\nThe pipeline uses `line-list` topology with depth testing on and no back-face\nculling. It expects the same interleaved position and normal vertex layout as the\nsource mesh passed to `meshToWireframe`.\n\n`color` defaults to `[1, 1, 1]` in linear RGB. `targetFormat` defaults to\n`'bgra8unorm-srgb'`; use `'rgba8unorm-srgb'` on platforms that require it.\n\nThe returned `InspectMaterial` is for low-level draws. Pair it with\n`meshToWireframe`, bind a uniform buffer at group 0, then issue an indexed\nline-list draw.\n"
    },
    {
      "package": "@vgpu/render/inspect",
      "symbol": "WireframeMesh",
      "repoPath": "packages/render/src/inspect/mesh-to-wireframe.docs.md",
      "virtualPath": "/@vgpu/render/inspect/mesh-to-wireframe.docs.md",
      "content": "# meshToWireframe\n\n`meshToWireframe(mesh, device)` converts a readable triangle-list `Mesh` into a\nline-list index buffer with duplicate edges removed by endpoint position.\n\n```ts\nimport { meshToWireframe } from \"@vgpu/render/inspect\";\n\nconst wireframe = await meshToWireframe(readableMesh, device);\n\n// wireframe.vertexBuffer is readableMesh.vertexBuffer\n// wireframe.indexBuffer contains two indices per line segment\n```\n\nThe source mesh's vertex buffer must be created with `GPUBufferUsage.COPY_SRC`\nso `meshToWireframe` can read back vertex positions. If the buffer is not\nreadable, the function throws `VGPU-CORE-INVALID-USAGE`; `Mesh.box` currently\ndoes not satisfy this contract.\n\nEndpoints are quantized to a `1e-6` grid before comparison. Meshes with distinct\nfeatures below that scale may have edges incorrectly merged, so author debug\ngeometry at a larger scale or use a precomputed wireframe for tiny details.\n\nCoplanar triangle diagonals are omitted by comparing triangle face normals, so a\nreadable unit-box triangle list produces 12 line segments and 24 indices.\n\nThe returned `indexBuffer` is a raw `GPUBuffer`. Callers own its lifetime and\nshould call `indexBuffer.destroy()` when done, or rely on `device.destroy()` to\nclean up at teardown.\n"
    },
    {
      "package": "@vgpu/render/passes",
      "symbol": "renderTargetForCanvas",
      "repoPath": "packages/render/src/render-target/render-target-canvas.docs.md",
      "virtualPath": "/@vgpu/render/passes/render-target-canvas.docs.md",
      "content": "# renderTargetForCanvas\n\n`renderTargetForCanvas(context, options?)` adapts the current texture from a\n`GPUCanvasContext` into a render target that can be passed to `pass()` and other\n`@vgpu/render/passes` helpers.\n\nThe returned target resolves its `color` view lazily each time it is read, so it\ntracks `context.getCurrentTexture()` across frames. Use `label` and `clearColor`\nin the options object to configure the generated attachment metadata and default\nclear color.\n\n```ts\nconst target = renderTargetForCanvas(canvasContext, { label: \"screen\" });\npass({ mesh, material, target });\n```\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "canvasMouseTracker",
      "repoPath": "packages/render/src/utils/canvas-mouse-tracker.docs.md",
      "virtualPath": "/@vgpu/render/utils/canvas-mouse-tracker.docs.md",
      "content": "# canvasMouseTracker\n\nTracks the latest pointer position over a canvas. Use it when a render loop needs the mouse position without storing event handlers in your app code.\n\n```ts\nconst mouse = canvasMouseTracker({ canvas, normalize: true });\nfunction draw() {\n  const [x, y] = mouse.position;\n  material.writeUniforms({ mouse: [x, y] });\n}\n```\n\nCall `dispose()` when the canvas is removed.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "CanvasMouseTracker",
      "repoPath": "packages/render/src/utils/canvas-mouse-tracker.docs.md",
      "virtualPath": "/@vgpu/render/utils/canvas-mouse-tracker.docs.md",
      "content": "# canvasMouseTracker\n\nTracks the latest pointer position over a canvas. Use it when a render loop needs the mouse position without storing event handlers in your app code.\n\n```ts\nconst mouse = canvasMouseTracker({ canvas, normalize: true });\nfunction draw() {\n  const [x, y] = mouse.position;\n  material.writeUniforms({ mouse: [x, y] });\n}\n```\n\nCall `dispose()` when the canvas is removed.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "CanvasMouseTrackerSpec",
      "repoPath": "packages/render/src/utils/canvas-mouse-tracker.docs.md",
      "virtualPath": "/@vgpu/render/utils/canvas-mouse-tracker.docs.md",
      "content": "# canvasMouseTracker\n\nTracks the latest pointer position over a canvas. Use it when a render loop needs the mouse position without storing event handlers in your app code.\n\n```ts\nconst mouse = canvasMouseTracker({ canvas, normalize: true });\nfunction draw() {\n  const [x, y] = mouse.position;\n  material.writeUniforms({ mouse: [x, y] });\n}\n```\n\nCall `dispose()` when the canvas is removed.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "canvasResolution",
      "repoPath": "packages/render/src/utils/canvas-resolution.docs.md",
      "virtualPath": "/@vgpu/render/utils/canvas-resolution.docs.md",
      "content": "# canvasResolution\n\nReads the drawing size of a canvas. Use it when shaders or render targets need the current canvas width and height.\n\n```ts\nconst size = canvasResolution(canvas, { observe: true });\nfunction draw() {\n  material.writeUniforms({ resolution: [size.width, size.height] });\n  renderer.draw({ target, material, mesh });\n}\n```\n\nCall `dispose()` to stop observing size changes.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "CanvasResolution",
      "repoPath": "packages/render/src/utils/canvas-resolution.docs.md",
      "virtualPath": "/@vgpu/render/utils/canvas-resolution.docs.md",
      "content": "# canvasResolution\n\nReads the drawing size of a canvas. Use it when shaders or render targets need the current canvas width and height.\n\n```ts\nconst size = canvasResolution(canvas, { observe: true });\nfunction draw() {\n  material.writeUniforms({ resolution: [size.width, size.height] });\n  renderer.draw({ target, material, mesh });\n}\n```\n\nCall `dispose()` to stop observing size changes.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "frameClock",
      "repoPath": "packages/render/src/utils/frame-clock.docs.md",
      "virtualPath": "/@vgpu/render/utils/frame-clock.docs.md",
      "content": "# frameClock\n\nMeasures elapsed time and frame delta in seconds. Use it in animation loops that need stable time values and pause support.\n\n```ts\nconst clock = frameClock();\nfunction frame() {\n  const time = clock.now();\n  const dt = clock.delta();\n  update(time, dt);\n}\n```\n\nUse `pause()`, `resume()`, and `reset()` to control the clock.\n"
    },
    {
      "package": "@vgpu/render/utils",
      "symbol": "FrameClock",
      "repoPath": "packages/render/src/utils/frame-clock.docs.md",
      "virtualPath": "/@vgpu/render/utils/frame-clock.docs.md",
      "content": "# frameClock\n\nMeasures elapsed time and frame delta in seconds. Use it in animation loops that need stable time values and pause support.\n\n```ts\nconst clock = frameClock();\nfunction frame() {\n  const time = clock.now();\n  const dt = clock.delta();\n  update(time, dt);\n}\n```\n\nUse `pause()`, `resume()`, and `reset()` to control the clock.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "Camera",
      "repoPath": "packages/render/src/domain/camera.docs.md",
      "virtualPath": "/@vgpu/render/camera.docs.md",
      "content": "# Camera\n\nA `Camera` is a plain value that contains the data shaders need to draw from a viewpoint.\n\nIt has a `viewProjectionMatrix`, a 16-element column-major `Float32Array`, and a `position`, a 3-element vector in world space. Camera factories compute the matrix once when you create the camera.\n\nThe returned camera object is frozen, so its fields cannot be replaced. The typed-array buffers are not frozen; mutating them after creation is undefined behavior. Copy a matrix or position first if you need your own editable snapshot.\n\n```ts\nimport { perspectiveCamera, degToRad } from \"@vgpu/render\";\n\nconst camera = perspectiveCamera({\n  fovYRadians: degToRad(45),\n  aspect: 1,\n  near: 0.1,\n  far: 100,\n  position: [2, 2, 3],\n  target: [0, 0, 0],\n});\n// camera.viewProjectionMatrix is a 16-element column-major Float32Array.\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "ColorAttachment",
      "repoPath": "packages/render/src/RenderPass.docs.md",
      "virtualPath": "/@vgpu/render/RenderPass.docs.md",
      "content": "# RenderPass\n\n`RenderPass` is a command-encoding wrapper for drawing into color\nattachments. Construct it with a core `Device` and `RenderPassOptions`, encode\nrender commands, then call `end()` to finish the pass and submit the command\nbuffer through `Device.queue`.\n\n`RenderPassOptions` contains `colorAttachments` and an optional `label`.\n`ColorAttachment.view` accepts either a core `Texture` or a raw\n`GPUTextureView`; `loadOp`, `storeOp`, and `clearValue` are forwarded to WebGPU.\n\nCommands mirror the WebGPU render pass encoder:\n\n- `setPipeline(pipeline)` binds a raw `GPURenderPipeline`.\n- `setBindGroup(index, group, dynamicOffsets?)` binds a raw `GPUBindGroup`.\n- `setVertexBuffer(slot, buffer, offset?, size?)` binds a core `Buffer` or raw\n  `GPUBuffer` for vertex input.\n- `draw({ vertexCount, instanceCount?, firstVertex?, firstInstance? })` issues a\n  non-indexed draw. The numeric WebGPU-style `draw(vertexCount, ...)` call is\n  also accepted.\n\nUse `gpu` when you need direct `GPURenderPassEncoder` access for commands that\nare not wrapped yet.\n\nInvariants: a pass is single-use. After `end()`, the underlying\n`GPURenderPassEncoder` is invalid, so `RenderPass.gpu` throws `VGPUError` with\ncode `VGPU-RENDER-PASS-ENDED`. Calling `end()` more than once is safe and is a\nno-op after the first submission.\n\nExample:\n\n```ts\nconst pass = new RenderPass(device, {\n  colorAttachments: [{ view: target, loadOp: \"clear\", storeOp: \"store\", clearValue: [0, 0, 0, 1] }],\n});\npass.setPipeline(pipeline);\npass.setBindGroup(0, uniforms);\npass.setVertexBuffer(0, vertices);\npass.draw({ vertexCount: 3 });\npass.end();\n```\n\n## Multiple render targets\n\n`@vgpu/render/passes` supports offscreen MRT through `renderTargetMulti()` or the\n`renderTarget({ colors })` overload. Each color attachment declares its own\nformat, optional label, and default clear color; depth is shared and MSAA remains\nsingle-color only in this release.\n\n```ts\nconst gbuffer = await renderTargetMulti({\n  device,\n  size: [1024, 768],\n  colors: [\n    { format: \"rgba8unorm\", label: \"albedo\" },\n    { format: \"rgba8unorm\", label: \"normal\", clearColor: [0.5, 0.5, 1, 1] },\n  ],\n  depth: true,\n});\n\npass({ mesh, material: gbufferMaterial, target: gbuffer });\npass({\n  mesh: fullscreenQuad,\n  material: compositeMaterial(gbuffer.colors[0], gbuffer.colors[1]),\n  target: finalColor,\n});\n```\n\n`pass().clearColor` accepts either a scalar color, which broadcasts to every MRT\nattachment, or an array whose length matches `target.colors.length`. Scalar\n`colorLoadOp`, `depthLoadOp`, and `depthClearValue` are also broadcast/shared.\n\n```ts\npass({\n  mesh,\n  material: pickingMaterial,\n  target: pickingTarget,\n  clearColor: [\n    [0, 0, 0, 1], // visible color\n    [0, 0, 0, 1], // entity-id buffer, packed into rgba8unorm for snapshots\n  ],\n});\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "createRenderPipeline",
      "repoPath": "packages/render/src/createRenderPipeline.docs.md",
      "virtualPath": "/@vgpu/render/createRenderPipeline.docs.md",
      "content": "# createRenderPipeline\n\nCreates a GPU render pipeline from a vgpu `Shader` plus vertex and fragment\nentry points. Returns a raw `GPURenderPipeline` with no wrapper.\n\n## Signature\n\n`createRenderPipeline(device: Device, opts: RenderPipelineOptions): GPURenderPipeline`\n\n## Options\n\n- `shader`: the `Shader` whose compiled GPU module will back both stages.\n- `vertex.entry`: the vertex shader entry-point name.\n- `fragment.entry`: the fragment shader entry-point name.\n- `fragment.targets`: the color target formats and blend/write settings.\n- `primitive`: optional WebGPU primitive state such as topology or culling.\n- `layout`: optional pipeline layout, or `\"auto\"` to let WebGPU derive one.\n- `label`: optional debug label forwarded to WebGPU.\n\n## Example\n\n```ts\nconst pipeline = createRenderPipeline(device, {\n  shader,\n  vertex: { entry: \"vs_main\" },\n  fragment: { entry: \"fs_main\", targets: [{ format: \"rgba8unorm\" }] },\n  primitive: { topology: \"triangle-list\" },\n});\n```\n\n## Notes\n\nThe returned `GPURenderPipeline` can be used directly with WebGPU APIs or\npassed to `RenderPass.setPipeline`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "degToRad",
      "repoPath": "packages/render/src/domain/deg-to-rad.docs.md",
      "virtualPath": "/@vgpu/render/deg-to-rad.docs.md",
      "content": "# degToRad\n\nConverts degrees to radians for numeric inputs such as camera field-of-view values.\n\nUse it when a design, note, or UI control gives an angle in degrees but the render API expects radians:\n\n```ts\nconst fovYRadians = degToRad(50);\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "DrawSpec",
      "repoPath": "packages/render/src/rapid-renderer.docs.md",
      "virtualPath": "/@vgpu/render/rapid-renderer.docs.md",
      "content": "# RapidRenderer\n\n`RapidRenderer` submits one draw to a target view. The renderer binds a material,\noptionally binds a mesh, then draws either `vertexCount` or `mesh.vertexCount`.\n\n```ts\nmaterial.writeUniforms({ viewProjection, model, cameraPosition, light });\nawait renderer.draw({ material, mesh, target, depthTarget });\nawait renderer.draw({ material, pipeline, target, vertexCount: 3 });\n```\n\n`material` is required. `pipeline` is an optional override; otherwise the renderer\nuses `material.pipeline`. Call `material.writeUniforms()` before `draw()` when the\nmaterial has uniforms. The renderer does not write camera, transform, light, or\nmaterial parameters.\n\n`clearValue` defaults to opaque black. `depthTarget` is optional. `renderer.gpu`\nreturns the raw `GPUDevice`. `draw()` resolves after command submission.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "Mat4",
      "repoPath": "packages/render/src/domain/camera.docs.md",
      "virtualPath": "/@vgpu/render/camera.docs.md",
      "content": "# Camera\n\nA `Camera` is a plain value that contains the data shaders need to draw from a viewpoint.\n\nIt has a `viewProjectionMatrix`, a 16-element column-major `Float32Array`, and a `position`, a 3-element vector in world space. Camera factories compute the matrix once when you create the camera.\n\nThe returned camera object is frozen, so its fields cannot be replaced. The typed-array buffers are not frozen; mutating them after creation is undefined behavior. Copy a matrix or position first if you need your own editable snapshot.\n\n```ts\nimport { perspectiveCamera, degToRad } from \"@vgpu/render\";\n\nconst camera = perspectiveCamera({\n  fovYRadians: degToRad(45),\n  aspect: 1,\n  near: 0.1,\n  far: 100,\n  position: [2, 2, 3],\n  target: [0, 0, 0],\n});\n// camera.viewProjectionMatrix is a 16-element column-major Float32Array.\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "Material",
      "repoPath": "packages/render/src/domain/material.docs.md",
      "virtualPath": "/@vgpu/render/material.docs.md",
      "content": "# Material\n\nA `Material` is the GPU state needed to draw a mesh with a shader. It contains:\n\n- `pipeline`: the ready-to-bind `GPURenderPipeline`.\n- `bindGroupLayout`: the layout for per-draw uniforms at group 0.\n- `shader`: the compiled vgpu shader object.\n- `uniformByteSize`: the number of bytes each uniform record must provide.\n\nUse the `material()` factory to build custom materials from WGSL vertex and fragment entry points.\n\n`MaterialUniformValue` is the value shape accepted by generic material uniform writers: a number, readonly number array, or typed numeric array.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "MaterialUniformValue",
      "repoPath": "packages/render/src/domain/material.docs.md",
      "virtualPath": "/@vgpu/render/material.docs.md",
      "content": "# Material\n\nA `Material` is the GPU state needed to draw a mesh with a shader. It contains:\n\n- `pipeline`: the ready-to-bind `GPURenderPipeline`.\n- `bindGroupLayout`: the layout for per-draw uniforms at group 0.\n- `shader`: the compiled vgpu shader object.\n- `uniformByteSize`: the number of bytes each uniform record must provide.\n\nUse the `material()` factory to build custom materials from WGSL vertex and fragment entry points.\n\n`MaterialUniformValue` is the value shape accepted by generic material uniform writers: a number, readonly number array, or typed numeric array.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "Mesh",
      "repoPath": "packages/render/src/domain/mesh.docs.md",
      "virtualPath": "/@vgpu/render/mesh.docs.md",
      "content": "# Mesh\n\n`Mesh` is a pure geometry value for draw code. It owns a vertex buffer, a vertex count, a vertex\nattribute description, and a bounding box. It does not store transforms or scene hierarchy data.\n\n## `Mesh.box({ device, size })`\n\n`Mesh.box({ device })` returns a unit cube with face normals suitable for flat-shaded lighting. The\ndefault size is `1`, so the bounding box spans `[-0.5, -0.5, -0.5]` to `[0.5, 0.5, 0.5]`.\n\nPass `size` to build a cube with a different edge length:\n\n```ts\nconst unit = Mesh.box({ device });\nconst large = Mesh.box({ device, size: 2 });\n```\n\nThe vertex layout is interleaved as `[position, normal]` with two `vec3<f32>` attributes:\n\n- stride: 24 bytes\n- position: offset 0, format `float32x3`\n- normal: offset 12, format `float32x3`\n\nThe cube uses 36 vertices: six faces, two triangles per face, and three vertices per triangle. Faces\ndo not share vertices, so each face has one constant normal.\n\nMeshes are cached per device and per size. Repeating `Mesh.box({ device, size: 1 })` returns the same\nmesh instance and reuses one GPU vertex buffer for that device.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "orthographicCamera",
      "repoPath": "packages/render/src/domain/orthographic-camera.docs.md",
      "virtualPath": "/@vgpu/render/orthographic-camera.docs.md",
      "content": "# orthographicCamera\n\nCreates a pure camera value for orthographic projection.\n\nPass the left, right, bottom, top, near, and far planes, plus a world-space `position` and `target`. The optional `up` vector defaults to `[0, 1, 0]`.\n\n```ts\nimport { orthographicCamera } from \"@vgpu/render\";\n\nconst camera = orthographicCamera({\n  left: -1,\n  right: 1,\n  bottom: -1,\n  top: 1,\n  near: 0.1,\n  far: 100,\n  position: new Float32Array([0, 0, 5]),\n  target: new Float32Array([0, 0, 0]),\n});\n```\n\nThe matrix is computed once as projection times view and stored on `camera.viewProjectionMatrix`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "perspectiveCamera",
      "repoPath": "packages/render/src/domain/perspective-camera.docs.md",
      "virtualPath": "/@vgpu/render/perspective-camera.docs.md",
      "content": "# perspectiveCamera\n\nCreates a pure camera value for perspective projection.\n\nPass a vertical field of view in radians, aspect ratio, near and far planes, and a world-space `position` and `target`. The optional `up` vector defaults to `[0, 1, 0]`.\n\n```ts\nimport { degToRad, perspectiveCamera } from \"@vgpu/render\";\n\nconst camera = perspectiveCamera({\n  fovYRadians: degToRad(45),\n  aspect: canvas.width / canvas.height,\n  near: 0.1,\n  far: 100,\n  position: new Float32Array([0, 0, 5]),\n  target: new Float32Array([0, 0, 0]),\n});\n```\n\nThe matrix is computed once as projection times view and stored on `camera.viewProjectionMatrix`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RapidRenderer",
      "repoPath": "packages/render/src/rapid-renderer.docs.md",
      "virtualPath": "/@vgpu/render/rapid-renderer.docs.md",
      "content": "# RapidRenderer\n\n`RapidRenderer` submits one draw to a target view. The renderer binds a material,\noptionally binds a mesh, then draws either `vertexCount` or `mesh.vertexCount`.\n\n```ts\nmaterial.writeUniforms({ viewProjection, model, cameraPosition, light });\nawait renderer.draw({ material, mesh, target, depthTarget });\nawait renderer.draw({ material, pipeline, target, vertexCount: 3 });\n```\n\n`material` is required. `pipeline` is an optional override; otherwise the renderer\nuses `material.pipeline`. Call `material.writeUniforms()` before `draw()` when the\nmaterial has uniforms. The renderer does not write camera, transform, light, or\nmaterial parameters.\n\n`clearValue` defaults to opaque black. `depthTarget` is optional. `renderer.gpu`\nreturns the raw `GPUDevice`. `draw()` resolves after command submission.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RenderPassDrawOptions",
      "repoPath": "packages/render/src/RenderPass.docs.md",
      "virtualPath": "/@vgpu/render/RenderPass.docs.md",
      "content": "# RenderPass\n\n`RenderPass` is a command-encoding wrapper for drawing into color\nattachments. Construct it with a core `Device` and `RenderPassOptions`, encode\nrender commands, then call `end()` to finish the pass and submit the command\nbuffer through `Device.queue`.\n\n`RenderPassOptions` contains `colorAttachments` and an optional `label`.\n`ColorAttachment.view` accepts either a core `Texture` or a raw\n`GPUTextureView`; `loadOp`, `storeOp`, and `clearValue` are forwarded to WebGPU.\n\nCommands mirror the WebGPU render pass encoder:\n\n- `setPipeline(pipeline)` binds a raw `GPURenderPipeline`.\n- `setBindGroup(index, group, dynamicOffsets?)` binds a raw `GPUBindGroup`.\n- `setVertexBuffer(slot, buffer, offset?, size?)` binds a core `Buffer` or raw\n  `GPUBuffer` for vertex input.\n- `draw({ vertexCount, instanceCount?, firstVertex?, firstInstance? })` issues a\n  non-indexed draw. The numeric WebGPU-style `draw(vertexCount, ...)` call is\n  also accepted.\n\nUse `gpu` when you need direct `GPURenderPassEncoder` access for commands that\nare not wrapped yet.\n\nInvariants: a pass is single-use. After `end()`, the underlying\n`GPURenderPassEncoder` is invalid, so `RenderPass.gpu` throws `VGPUError` with\ncode `VGPU-RENDER-PASS-ENDED`. Calling `end()` more than once is safe and is a\nno-op after the first submission.\n\nExample:\n\n```ts\nconst pass = new RenderPass(device, {\n  colorAttachments: [{ view: target, loadOp: \"clear\", storeOp: \"store\", clearValue: [0, 0, 0, 1] }],\n});\npass.setPipeline(pipeline);\npass.setBindGroup(0, uniforms);\npass.setVertexBuffer(0, vertices);\npass.draw({ vertexCount: 3 });\npass.end();\n```\n\n## Multiple render targets\n\n`@vgpu/render/passes` supports offscreen MRT through `renderTargetMulti()` or the\n`renderTarget({ colors })` overload. Each color attachment declares its own\nformat, optional label, and default clear color; depth is shared and MSAA remains\nsingle-color only in this release.\n\n```ts\nconst gbuffer = await renderTargetMulti({\n  device,\n  size: [1024, 768],\n  colors: [\n    { format: \"rgba8unorm\", label: \"albedo\" },\n    { format: \"rgba8unorm\", label: \"normal\", clearColor: [0.5, 0.5, 1, 1] },\n  ],\n  depth: true,\n});\n\npass({ mesh, material: gbufferMaterial, target: gbuffer });\npass({\n  mesh: fullscreenQuad,\n  material: compositeMaterial(gbuffer.colors[0], gbuffer.colors[1]),\n  target: finalColor,\n});\n```\n\n`pass().clearColor` accepts either a scalar color, which broadcasts to every MRT\nattachment, or an array whose length matches `target.colors.length`. Scalar\n`colorLoadOp`, `depthLoadOp`, and `depthClearValue` are also broadcast/shared.\n\n```ts\npass({\n  mesh,\n  material: pickingMaterial,\n  target: pickingTarget,\n  clearColor: [\n    [0, 0, 0, 1], // visible color\n    [0, 0, 0, 1], // entity-id buffer, packed into rgba8unorm for snapshots\n  ],\n});\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RenderPassDynamicOffsets",
      "repoPath": "packages/render/src/RenderPass.docs.md",
      "virtualPath": "/@vgpu/render/RenderPass.docs.md",
      "content": "# RenderPass\n\n`RenderPass` is a command-encoding wrapper for drawing into color\nattachments. Construct it with a core `Device` and `RenderPassOptions`, encode\nrender commands, then call `end()` to finish the pass and submit the command\nbuffer through `Device.queue`.\n\n`RenderPassOptions` contains `colorAttachments` and an optional `label`.\n`ColorAttachment.view` accepts either a core `Texture` or a raw\n`GPUTextureView`; `loadOp`, `storeOp`, and `clearValue` are forwarded to WebGPU.\n\nCommands mirror the WebGPU render pass encoder:\n\n- `setPipeline(pipeline)` binds a raw `GPURenderPipeline`.\n- `setBindGroup(index, group, dynamicOffsets?)` binds a raw `GPUBindGroup`.\n- `setVertexBuffer(slot, buffer, offset?, size?)` binds a core `Buffer` or raw\n  `GPUBuffer` for vertex input.\n- `draw({ vertexCount, instanceCount?, firstVertex?, firstInstance? })` issues a\n  non-indexed draw. The numeric WebGPU-style `draw(vertexCount, ...)` call is\n  also accepted.\n\nUse `gpu` when you need direct `GPURenderPassEncoder` access for commands that\nare not wrapped yet.\n\nInvariants: a pass is single-use. After `end()`, the underlying\n`GPURenderPassEncoder` is invalid, so `RenderPass.gpu` throws `VGPUError` with\ncode `VGPU-RENDER-PASS-ENDED`. Calling `end()` more than once is safe and is a\nno-op after the first submission.\n\nExample:\n\n```ts\nconst pass = new RenderPass(device, {\n  colorAttachments: [{ view: target, loadOp: \"clear\", storeOp: \"store\", clearValue: [0, 0, 0, 1] }],\n});\npass.setPipeline(pipeline);\npass.setBindGroup(0, uniforms);\npass.setVertexBuffer(0, vertices);\npass.draw({ vertexCount: 3 });\npass.end();\n```\n\n## Multiple render targets\n\n`@vgpu/render/passes` supports offscreen MRT through `renderTargetMulti()` or the\n`renderTarget({ colors })` overload. Each color attachment declares its own\nformat, optional label, and default clear color; depth is shared and MSAA remains\nsingle-color only in this release.\n\n```ts\nconst gbuffer = await renderTargetMulti({\n  device,\n  size: [1024, 768],\n  colors: [\n    { format: \"rgba8unorm\", label: \"albedo\" },\n    { format: \"rgba8unorm\", label: \"normal\", clearColor: [0.5, 0.5, 1, 1] },\n  ],\n  depth: true,\n});\n\npass({ mesh, material: gbufferMaterial, target: gbuffer });\npass({\n  mesh: fullscreenQuad,\n  material: compositeMaterial(gbuffer.colors[0], gbuffer.colors[1]),\n  target: finalColor,\n});\n```\n\n`pass().clearColor` accepts either a scalar color, which broadcasts to every MRT\nattachment, or an array whose length matches `target.colors.length`. Scalar\n`colorLoadOp`, `depthLoadOp`, and `depthClearValue` are also broadcast/shared.\n\n```ts\npass({\n  mesh,\n  material: pickingMaterial,\n  target: pickingTarget,\n  clearColor: [\n    [0, 0, 0, 1], // visible color\n    [0, 0, 0, 1], // entity-id buffer, packed into rgba8unorm for snapshots\n  ],\n});\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RenderPassOptions",
      "repoPath": "packages/render/src/RenderPass.docs.md",
      "virtualPath": "/@vgpu/render/RenderPass.docs.md",
      "content": "# RenderPass\n\n`RenderPass` is a command-encoding wrapper for drawing into color\nattachments. Construct it with a core `Device` and `RenderPassOptions`, encode\nrender commands, then call `end()` to finish the pass and submit the command\nbuffer through `Device.queue`.\n\n`RenderPassOptions` contains `colorAttachments` and an optional `label`.\n`ColorAttachment.view` accepts either a core `Texture` or a raw\n`GPUTextureView`; `loadOp`, `storeOp`, and `clearValue` are forwarded to WebGPU.\n\nCommands mirror the WebGPU render pass encoder:\n\n- `setPipeline(pipeline)` binds a raw `GPURenderPipeline`.\n- `setBindGroup(index, group, dynamicOffsets?)` binds a raw `GPUBindGroup`.\n- `setVertexBuffer(slot, buffer, offset?, size?)` binds a core `Buffer` or raw\n  `GPUBuffer` for vertex input.\n- `draw({ vertexCount, instanceCount?, firstVertex?, firstInstance? })` issues a\n  non-indexed draw. The numeric WebGPU-style `draw(vertexCount, ...)` call is\n  also accepted.\n\nUse `gpu` when you need direct `GPURenderPassEncoder` access for commands that\nare not wrapped yet.\n\nInvariants: a pass is single-use. After `end()`, the underlying\n`GPURenderPassEncoder` is invalid, so `RenderPass.gpu` throws `VGPUError` with\ncode `VGPU-RENDER-PASS-ENDED`. Calling `end()` more than once is safe and is a\nno-op after the first submission.\n\nExample:\n\n```ts\nconst pass = new RenderPass(device, {\n  colorAttachments: [{ view: target, loadOp: \"clear\", storeOp: \"store\", clearValue: [0, 0, 0, 1] }],\n});\npass.setPipeline(pipeline);\npass.setBindGroup(0, uniforms);\npass.setVertexBuffer(0, vertices);\npass.draw({ vertexCount: 3 });\npass.end();\n```\n\n## Multiple render targets\n\n`@vgpu/render/passes` supports offscreen MRT through `renderTargetMulti()` or the\n`renderTarget({ colors })` overload. Each color attachment declares its own\nformat, optional label, and default clear color; depth is shared and MSAA remains\nsingle-color only in this release.\n\n```ts\nconst gbuffer = await renderTargetMulti({\n  device,\n  size: [1024, 768],\n  colors: [\n    { format: \"rgba8unorm\", label: \"albedo\" },\n    { format: \"rgba8unorm\", label: \"normal\", clearColor: [0.5, 0.5, 1, 1] },\n  ],\n  depth: true,\n});\n\npass({ mesh, material: gbufferMaterial, target: gbuffer });\npass({\n  mesh: fullscreenQuad,\n  material: compositeMaterial(gbuffer.colors[0], gbuffer.colors[1]),\n  target: finalColor,\n});\n```\n\n`pass().clearColor` accepts either a scalar color, which broadcasts to every MRT\nattachment, or an array whose length matches `target.colors.length`. Scalar\n`colorLoadOp`, `depthLoadOp`, and `depthClearValue` are also broadcast/shared.\n\n```ts\npass({\n  mesh,\n  material: pickingMaterial,\n  target: pickingTarget,\n  clearColor: [\n    [0, 0, 0, 1], // visible color\n    [0, 0, 0, 1], // entity-id buffer, packed into rgba8unorm for snapshots\n  ],\n});\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RenderPass",
      "repoPath": "packages/render/src/RenderPass.docs.md",
      "virtualPath": "/@vgpu/render/RenderPass.docs.md",
      "content": "# RenderPass\n\n`RenderPass` is a command-encoding wrapper for drawing into color\nattachments. Construct it with a core `Device` and `RenderPassOptions`, encode\nrender commands, then call `end()` to finish the pass and submit the command\nbuffer through `Device.queue`.\n\n`RenderPassOptions` contains `colorAttachments` and an optional `label`.\n`ColorAttachment.view` accepts either a core `Texture` or a raw\n`GPUTextureView`; `loadOp`, `storeOp`, and `clearValue` are forwarded to WebGPU.\n\nCommands mirror the WebGPU render pass encoder:\n\n- `setPipeline(pipeline)` binds a raw `GPURenderPipeline`.\n- `setBindGroup(index, group, dynamicOffsets?)` binds a raw `GPUBindGroup`.\n- `setVertexBuffer(slot, buffer, offset?, size?)` binds a core `Buffer` or raw\n  `GPUBuffer` for vertex input.\n- `draw({ vertexCount, instanceCount?, firstVertex?, firstInstance? })` issues a\n  non-indexed draw. The numeric WebGPU-style `draw(vertexCount, ...)` call is\n  also accepted.\n\nUse `gpu` when you need direct `GPURenderPassEncoder` access for commands that\nare not wrapped yet.\n\nInvariants: a pass is single-use. After `end()`, the underlying\n`GPURenderPassEncoder` is invalid, so `RenderPass.gpu` throws `VGPUError` with\ncode `VGPU-RENDER-PASS-ENDED`. Calling `end()` more than once is safe and is a\nno-op after the first submission.\n\nExample:\n\n```ts\nconst pass = new RenderPass(device, {\n  colorAttachments: [{ view: target, loadOp: \"clear\", storeOp: \"store\", clearValue: [0, 0, 0, 1] }],\n});\npass.setPipeline(pipeline);\npass.setBindGroup(0, uniforms);\npass.setVertexBuffer(0, vertices);\npass.draw({ vertexCount: 3 });\npass.end();\n```\n\n## Multiple render targets\n\n`@vgpu/render/passes` supports offscreen MRT through `renderTargetMulti()` or the\n`renderTarget({ colors })` overload. Each color attachment declares its own\nformat, optional label, and default clear color; depth is shared and MSAA remains\nsingle-color only in this release.\n\n```ts\nconst gbuffer = await renderTargetMulti({\n  device,\n  size: [1024, 768],\n  colors: [\n    { format: \"rgba8unorm\", label: \"albedo\" },\n    { format: \"rgba8unorm\", label: \"normal\", clearColor: [0.5, 0.5, 1, 1] },\n  ],\n  depth: true,\n});\n\npass({ mesh, material: gbufferMaterial, target: gbuffer });\npass({\n  mesh: fullscreenQuad,\n  material: compositeMaterial(gbuffer.colors[0], gbuffer.colors[1]),\n  target: finalColor,\n});\n```\n\n`pass().clearColor` accepts either a scalar color, which broadcasts to every MRT\nattachment, or an array whose length matches `target.colors.length`. Scalar\n`colorLoadOp`, `depthLoadOp`, and `depthClearValue` are also broadcast/shared.\n\n```ts\npass({\n  mesh,\n  material: pickingMaterial,\n  target: pickingTarget,\n  clearColor: [\n    [0, 0, 0, 1], // visible color\n    [0, 0, 0, 1], // entity-id buffer, packed into rgba8unorm for snapshots\n  ],\n});\n```\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "RenderPipelineOptions",
      "repoPath": "packages/render/src/createRenderPipeline.docs.md",
      "virtualPath": "/@vgpu/render/createRenderPipeline.docs.md",
      "content": "# createRenderPipeline\n\nCreates a GPU render pipeline from a vgpu `Shader` plus vertex and fragment\nentry points. Returns a raw `GPURenderPipeline` with no wrapper.\n\n## Signature\n\n`createRenderPipeline(device: Device, opts: RenderPipelineOptions): GPURenderPipeline`\n\n## Options\n\n- `shader`: the `Shader` whose compiled GPU module will back both stages.\n- `vertex.entry`: the vertex shader entry-point name.\n- `fragment.entry`: the fragment shader entry-point name.\n- `fragment.targets`: the color target formats and blend/write settings.\n- `primitive`: optional WebGPU primitive state such as topology or culling.\n- `layout`: optional pipeline layout, or `\"auto\"` to let WebGPU derive one.\n- `label`: optional debug label forwarded to WebGPU.\n\n## Example\n\n```ts\nconst pipeline = createRenderPipeline(device, {\n  shader,\n  vertex: { entry: \"vs_main\" },\n  fragment: { entry: \"fs_main\", targets: [{ format: \"rgba8unorm\" }] },\n  primitive: { topology: \"triangle-list\" },\n});\n```\n\n## Notes\n\nThe returned `GPURenderPipeline` can be used directly with WebGPU APIs or\npassed to `RenderPass.setPipeline`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "srgb",
      "repoPath": "packages/render/src/domain/srgb.docs.md",
      "virtualPath": "/@vgpu/render/srgb.docs.md",
      "content": "# srgb\n\nConverts colors from sRGB into linear RGB.\n\nThe input is sRGB: the way humans, design tools, and hex color pickers usually describe colors. The output is linear RGB in the `0..1` range, which is the color space shaders need for lighting math.\n\n```ts\nconst baseColor = srgb(0xcc8844);\n```\n\nYou can pass a packed hex value such as `0xff6600`, or a three-component sRGB tuple such as `[0.5, 0.5, 0.5]`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "UniformLayout",
      "repoPath": "packages/render/src/uniform-pool.docs.md",
      "virtualPath": "/@vgpu/render/uniform-pool.docs.md",
      "content": "# UniformPool\n\n`UniformPool` stages per-draw uniform bytes in CPU memory and uploads them to a\nWebGPU uniform buffer at the frame boundary. Each `push()` returns the dynamic\noffset, in bytes, to pass when binding the slot for a draw.\n\n## Slots\n\n`pool.alloc(layout)` returns a `UniformSlot<T>` with:\n\n- `slot.gpu`: the underlying `GPUBuffer` used by the pool.\n- `slot.bindGroup`: a real `GPUBindGroup` with a dynamic uniform-buffer binding.\n- `slot.bindGroupLayout`: a real `GPUBindGroupLayout` whose binding uses\n  `hasDynamicOffset: true`.\n- `slot.stride`: `layout.size` padded up to the device's uniform-buffer offset\n  alignment.\n\nAll slots allocated by one pool share the same backing `GPUBuffer`. Each slot\nhas its own bind group so callers can bind the slot once and vary only the\ndynamic offset returned by `push()` or `pushBytes()`.\n\n## Pipeline layout sharing\n\nA layout may provide a pre-built bind group layout:\n\n```ts\nconst slot = pool.alloc({\n  size,\n  bindGroupLayout: material.bindGroupLayout,\n  encode(value, dst, byteOffset) {\n    // write uniform bytes\n  },\n});\n```\n\nWhen `bindGroupLayout` is provided, the slot reuses it instead of creating a new\nlayout. Use this when a pipeline already owns the bind group layout that will\nconsume the slot's bind group.\n\n## Frame uploads\n\n`push(value)` and `pushBytes(bytes)` write into the pool's CPU mirror only. They\ndo not call WebGPU immediately. `endFrame()` uploads pending bytes with\n`device.gpu.queue.writeBuffer(pool.gpu, 0, pool.cpuMirror, 0, pool.usedBytes)`.\n\nCalling `endFrame()` on a clean pool is a no-op. If more values are pushed after\n`endFrame()`, a later `endFrame()` uploads the updated CPU mirror again.\n\n## Submit readiness\n\nCall `endFrame()` before submitting command buffers that read data from the\npool. `assertReadyForSubmit(opName)` checks this invariant. If there are pending\npushes that have not been uploaded, it throws `VGPU-CORE-INVALID-USAGE` with:\n\n```txt\nUniformPool has unflushed pushes; call endFrame() before submitting.\n```\n\nRenderers should call `assertReadyForSubmit()` immediately before\n`queue.submit()`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "UniformPoolOptions",
      "repoPath": "packages/render/src/uniform-pool.docs.md",
      "virtualPath": "/@vgpu/render/uniform-pool.docs.md",
      "content": "# UniformPool\n\n`UniformPool` stages per-draw uniform bytes in CPU memory and uploads them to a\nWebGPU uniform buffer at the frame boundary. Each `push()` returns the dynamic\noffset, in bytes, to pass when binding the slot for a draw.\n\n## Slots\n\n`pool.alloc(layout)` returns a `UniformSlot<T>` with:\n\n- `slot.gpu`: the underlying `GPUBuffer` used by the pool.\n- `slot.bindGroup`: a real `GPUBindGroup` with a dynamic uniform-buffer binding.\n- `slot.bindGroupLayout`: a real `GPUBindGroupLayout` whose binding uses\n  `hasDynamicOffset: true`.\n- `slot.stride`: `layout.size` padded up to the device's uniform-buffer offset\n  alignment.\n\nAll slots allocated by one pool share the same backing `GPUBuffer`. Each slot\nhas its own bind group so callers can bind the slot once and vary only the\ndynamic offset returned by `push()` or `pushBytes()`.\n\n## Pipeline layout sharing\n\nA layout may provide a pre-built bind group layout:\n\n```ts\nconst slot = pool.alloc({\n  size,\n  bindGroupLayout: material.bindGroupLayout,\n  encode(value, dst, byteOffset) {\n    // write uniform bytes\n  },\n});\n```\n\nWhen `bindGroupLayout` is provided, the slot reuses it instead of creating a new\nlayout. Use this when a pipeline already owns the bind group layout that will\nconsume the slot's bind group.\n\n## Frame uploads\n\n`push(value)` and `pushBytes(bytes)` write into the pool's CPU mirror only. They\ndo not call WebGPU immediately. `endFrame()` uploads pending bytes with\n`device.gpu.queue.writeBuffer(pool.gpu, 0, pool.cpuMirror, 0, pool.usedBytes)`.\n\nCalling `endFrame()` on a clean pool is a no-op. If more values are pushed after\n`endFrame()`, a later `endFrame()` uploads the updated CPU mirror again.\n\n## Submit readiness\n\nCall `endFrame()` before submitting command buffers that read data from the\npool. `assertReadyForSubmit(opName)` checks this invariant. If there are pending\npushes that have not been uploaded, it throws `VGPU-CORE-INVALID-USAGE` with:\n\n```txt\nUniformPool has unflushed pushes; call endFrame() before submitting.\n```\n\nRenderers should call `assertReadyForSubmit()` immediately before\n`queue.submit()`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "UniformPool",
      "repoPath": "packages/render/src/uniform-pool.docs.md",
      "virtualPath": "/@vgpu/render/uniform-pool.docs.md",
      "content": "# UniformPool\n\n`UniformPool` stages per-draw uniform bytes in CPU memory and uploads them to a\nWebGPU uniform buffer at the frame boundary. Each `push()` returns the dynamic\noffset, in bytes, to pass when binding the slot for a draw.\n\n## Slots\n\n`pool.alloc(layout)` returns a `UniformSlot<T>` with:\n\n- `slot.gpu`: the underlying `GPUBuffer` used by the pool.\n- `slot.bindGroup`: a real `GPUBindGroup` with a dynamic uniform-buffer binding.\n- `slot.bindGroupLayout`: a real `GPUBindGroupLayout` whose binding uses\n  `hasDynamicOffset: true`.\n- `slot.stride`: `layout.size` padded up to the device's uniform-buffer offset\n  alignment.\n\nAll slots allocated by one pool share the same backing `GPUBuffer`. Each slot\nhas its own bind group so callers can bind the slot once and vary only the\ndynamic offset returned by `push()` or `pushBytes()`.\n\n## Pipeline layout sharing\n\nA layout may provide a pre-built bind group layout:\n\n```ts\nconst slot = pool.alloc({\n  size,\n  bindGroupLayout: material.bindGroupLayout,\n  encode(value, dst, byteOffset) {\n    // write uniform bytes\n  },\n});\n```\n\nWhen `bindGroupLayout` is provided, the slot reuses it instead of creating a new\nlayout. Use this when a pipeline already owns the bind group layout that will\nconsume the slot's bind group.\n\n## Frame uploads\n\n`push(value)` and `pushBytes(bytes)` write into the pool's CPU mirror only. They\ndo not call WebGPU immediately. `endFrame()` uploads pending bytes with\n`device.gpu.queue.writeBuffer(pool.gpu, 0, pool.cpuMirror, 0, pool.usedBytes)`.\n\nCalling `endFrame()` on a clean pool is a no-op. If more values are pushed after\n`endFrame()`, a later `endFrame()` uploads the updated CPU mirror again.\n\n## Submit readiness\n\nCall `endFrame()` before submitting command buffers that read data from the\npool. `assertReadyForSubmit(opName)` checks this invariant. If there are pending\npushes that have not been uploaded, it throws `VGPU-CORE-INVALID-USAGE` with:\n\n```txt\nUniformPool has unflushed pushes; call endFrame() before submitting.\n```\n\nRenderers should call `assertReadyForSubmit()` immediately before\n`queue.submit()`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "UniformSlot",
      "repoPath": "packages/render/src/uniform-pool.docs.md",
      "virtualPath": "/@vgpu/render/uniform-pool.docs.md",
      "content": "# UniformPool\n\n`UniformPool` stages per-draw uniform bytes in CPU memory and uploads them to a\nWebGPU uniform buffer at the frame boundary. Each `push()` returns the dynamic\noffset, in bytes, to pass when binding the slot for a draw.\n\n## Slots\n\n`pool.alloc(layout)` returns a `UniformSlot<T>` with:\n\n- `slot.gpu`: the underlying `GPUBuffer` used by the pool.\n- `slot.bindGroup`: a real `GPUBindGroup` with a dynamic uniform-buffer binding.\n- `slot.bindGroupLayout`: a real `GPUBindGroupLayout` whose binding uses\n  `hasDynamicOffset: true`.\n- `slot.stride`: `layout.size` padded up to the device's uniform-buffer offset\n  alignment.\n\nAll slots allocated by one pool share the same backing `GPUBuffer`. Each slot\nhas its own bind group so callers can bind the slot once and vary only the\ndynamic offset returned by `push()` or `pushBytes()`.\n\n## Pipeline layout sharing\n\nA layout may provide a pre-built bind group layout:\n\n```ts\nconst slot = pool.alloc({\n  size,\n  bindGroupLayout: material.bindGroupLayout,\n  encode(value, dst, byteOffset) {\n    // write uniform bytes\n  },\n});\n```\n\nWhen `bindGroupLayout` is provided, the slot reuses it instead of creating a new\nlayout. Use this when a pipeline already owns the bind group layout that will\nconsume the slot's bind group.\n\n## Frame uploads\n\n`push(value)` and `pushBytes(bytes)` write into the pool's CPU mirror only. They\ndo not call WebGPU immediately. `endFrame()` uploads pending bytes with\n`device.gpu.queue.writeBuffer(pool.gpu, 0, pool.cpuMirror, 0, pool.usedBytes)`.\n\nCalling `endFrame()` on a clean pool is a no-op. If more values are pushed after\n`endFrame()`, a later `endFrame()` uploads the updated CPU mirror again.\n\n## Submit readiness\n\nCall `endFrame()` before submitting command buffers that read data from the\npool. `assertReadyForSubmit(opName)` checks this invariant. If there are pending\npushes that have not been uploaded, it throws `VGPU-CORE-INVALID-USAGE` with:\n\n```txt\nUniformPool has unflushed pushes; call endFrame() before submitting.\n```\n\nRenderers should call `assertReadyForSubmit()` immediately before\n`queue.submit()`.\n"
    },
    {
      "package": "@vgpu/render",
      "symbol": "Vec3",
      "repoPath": "packages/render/src/domain/camera.docs.md",
      "virtualPath": "/@vgpu/render/camera.docs.md",
      "content": "# Camera\n\nA `Camera` is a plain value that contains the data shaders need to draw from a viewpoint.\n\nIt has a `viewProjectionMatrix`, a 16-element column-major `Float32Array`, and a `position`, a 3-element vector in world space. Camera factories compute the matrix once when you create the camera.\n\nThe returned camera object is frozen, so its fields cannot be replaced. The typed-array buffers are not frozen; mutating them after creation is undefined behavior. Copy a matrix or position first if you need your own editable snapshot.\n\n```ts\nimport { perspectiveCamera, degToRad } from \"@vgpu/render\";\n\nconst camera = perspectiveCamera({\n  fovYRadians: degToRad(45),\n  aspect: 1,\n  near: 0.1,\n  far: 100,\n  position: [2, 2, 3],\n  target: [0, 0, 0],\n});\n// camera.viewProjectionMatrix is a 16-element column-major Float32Array.\n```\n"
    },
    {
      "package": "@vgpu/wgsl/loader-vite",
      "symbol": "transformWgsl",
      "repoPath": "packages/wgsl/src/loader-vite/index.docs.md",
      "virtualPath": "/@vgpu/wgsl/loader-vite/index.docs.md",
      "content": "# wgslVitePlugin\n\n`wgslVitePlugin` lets Vite import `.wgsl` files as JavaScript string modules. Plain WGSL files are returned as raw source strings, while files with top-level vgpu-wgsl imports are resolved into one plain WGSL string during Vite transform.\n\n`transformWgsl(source, id)` is the testable transform primitive. It accepts the WGSL source and file id, and returns `ViteLoadResult` with JavaScript code plus `map: null`; the code default-exports the original or resolved WGSL string. The default plugin wraps that primitive with `{ name, transform }` and only handles paths ending in `.wgsl`.\n\nOptions are optional. `wgslVitePlugin()` and `transformWgsl(source, id)` preserve previous behavior, including byte-for-byte output for leaf shaders. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }`:\n\n```ts\nimport wgslVitePlugin, { transformWgsl } from \"@vgpu/wgsl/loader-vite\";\n\nexport default {\n  plugins: [wgslVitePlugin({ minify: true })],\n};\n\nconst whitespaceOnly = await transformWgsl(source, \"/shader.wgsl\", { minify: { whitespace: true } });\nconst safeIdentifiers = await transformWgsl(source, \"/shader.wgsl\", { minify: { identifiers: \"safe\" } });\n```\n\n`minify: true` is the production preset, equivalent to `{ whitespace: true, identifiers: \"safe\" }`. Object form defaults to whitespace on and identifiers off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\nSafe identifier shortening is AST/scope-aware and limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniforms/storage/texture/samplers, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, names are preserved. The minifier adds no dependency and does not produce compact-output source maps or struct-field renames yet.\n\nExisting import flattening and collision-avoidance mangling for imported shader graphs is unchanged. The transform calls the resolver with validation disabled; `resolveShader()` itself validates unminified output before diagnostics and validates the final output when identifiers are enabled.\n\nResolution failures, package export problems, invalid imports, and validation-disabled resolver errors surface as structured `VGPU-WGSL-*` exceptions from the transform. Vite catches those exceptions and presents them as transform failures. Module layout is configured through normal file paths, package `exports`, and resolver-compatible source files.\n"
    },
    {
      "package": "@vgpu/wgsl/loader-vite",
      "symbol": "ViteLoadResult",
      "repoPath": "packages/wgsl/src/loader-vite/index.docs.md",
      "virtualPath": "/@vgpu/wgsl/loader-vite/index.docs.md",
      "content": "# wgslVitePlugin\n\n`wgslVitePlugin` lets Vite import `.wgsl` files as JavaScript string modules. Plain WGSL files are returned as raw source strings, while files with top-level vgpu-wgsl imports are resolved into one plain WGSL string during Vite transform.\n\n`transformWgsl(source, id)` is the testable transform primitive. It accepts the WGSL source and file id, and returns `ViteLoadResult` with JavaScript code plus `map: null`; the code default-exports the original or resolved WGSL string. The default plugin wraps that primitive with `{ name, transform }` and only handles paths ending in `.wgsl`.\n\nOptions are optional. `wgslVitePlugin()` and `transformWgsl(source, id)` preserve previous behavior, including byte-for-byte output for leaf shaders. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }`:\n\n```ts\nimport wgslVitePlugin, { transformWgsl } from \"@vgpu/wgsl/loader-vite\";\n\nexport default {\n  plugins: [wgslVitePlugin({ minify: true })],\n};\n\nconst whitespaceOnly = await transformWgsl(source, \"/shader.wgsl\", { minify: { whitespace: true } });\nconst safeIdentifiers = await transformWgsl(source, \"/shader.wgsl\", { minify: { identifiers: \"safe\" } });\n```\n\n`minify: true` is the production preset, equivalent to `{ whitespace: true, identifiers: \"safe\" }`. Object form defaults to whitespace on and identifiers off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\nSafe identifier shortening is AST/scope-aware and limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniforms/storage/texture/samplers, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, names are preserved. The minifier adds no dependency and does not produce compact-output source maps or struct-field renames yet.\n\nExisting import flattening and collision-avoidance mangling for imported shader graphs is unchanged. The transform calls the resolver with validation disabled; `resolveShader()` itself validates unminified output before diagnostics and validates the final output when identifiers are enabled.\n\nResolution failures, package export problems, invalid imports, and validation-disabled resolver errors surface as structured `VGPU-WGSL-*` exceptions from the transform. Vite catches those exceptions and presents them as transform failures. Module layout is configured through normal file paths, package `exports`, and resolver-compatible source files.\n"
    },
    {
      "package": "@vgpu/wgsl/loader-vite",
      "symbol": "wgslVitePlugin",
      "repoPath": "packages/wgsl/src/loader-vite/index.docs.md",
      "virtualPath": "/@vgpu/wgsl/loader-vite/index.docs.md",
      "content": "# wgslVitePlugin\n\n`wgslVitePlugin` lets Vite import `.wgsl` files as JavaScript string modules. Plain WGSL files are returned as raw source strings, while files with top-level vgpu-wgsl imports are resolved into one plain WGSL string during Vite transform.\n\n`transformWgsl(source, id)` is the testable transform primitive. It accepts the WGSL source and file id, and returns `ViteLoadResult` with JavaScript code plus `map: null`; the code default-exports the original or resolved WGSL string. The default plugin wraps that primitive with `{ name, transform }` and only handles paths ending in `.wgsl`.\n\nOptions are optional. `wgslVitePlugin()` and `transformWgsl(source, id)` preserve previous behavior, including byte-for-byte output for leaf shaders. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }`:\n\n```ts\nimport wgslVitePlugin, { transformWgsl } from \"@vgpu/wgsl/loader-vite\";\n\nexport default {\n  plugins: [wgslVitePlugin({ minify: true })],\n};\n\nconst whitespaceOnly = await transformWgsl(source, \"/shader.wgsl\", { minify: { whitespace: true } });\nconst safeIdentifiers = await transformWgsl(source, \"/shader.wgsl\", { minify: { identifiers: \"safe\" } });\n```\n\n`minify: true` is the production preset, equivalent to `{ whitespace: true, identifiers: \"safe\" }`. Object form defaults to whitespace on and identifiers off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\nSafe identifier shortening is AST/scope-aware and limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniforms/storage/texture/samplers, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, names are preserved. The minifier adds no dependency and does not produce compact-output source maps or struct-field renames yet.\n\nExisting import flattening and collision-avoidance mangling for imported shader graphs is unchanged. The transform calls the resolver with validation disabled; `resolveShader()` itself validates unminified output before diagnostics and validates the final output when identifiers are enabled.\n\nResolution failures, package export problems, invalid imports, and validation-disabled resolver errors surface as structured `VGPU-WGSL-*` exceptions from the transform. Vite catches those exceptions and presents them as transform failures. Module layout is configured through normal file paths, package `exports`, and resolver-compatible source files.\n"
    },
    {
      "package": "@vgpu/wgsl/loader-webpack",
      "symbol": "wgslWebpackLoader",
      "repoPath": "packages/wgsl/src/loader-webpack/index.docs.md",
      "virtualPath": "/@vgpu/wgsl/loader-webpack/index.docs.md",
      "content": "# wgslWebpackLoader\n\n`wgslWebpackLoader` lets webpack-compatible bundlers import `.wgsl` files as JavaScript string modules. Plain WGSL files are emitted as `export default \"...\"`; files with top-level vgpu-wgsl imports are resolved to one plain WGSL string before webpack receives the module.\n\nThe loader input is the WGSL source and the webpack loader context, including `resourcePath`. It outputs JavaScript module source whose default export is the resolved WGSL string. Imported files are read relative to `resourcePath`, so normal webpack file watching should include the entry file while resolver errors identify missing or invalid imported modules.\n\nOptions are optional. By default `{ minify: false }` preserves previous behavior and emits leaf shaders byte-for-byte unchanged. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }`:\n\n```js\nmodule.exports = {\n  module: {\n    rules: [{ test: /\\.wgsl$/, loader: \"@vgpu/wgsl/loader-webpack\", options: { minify: true } }],\n  },\n};\n```\n\n`minify: true` is the production preset, equivalent to `{ whitespace: true, identifiers: \"safe\" }`. Object form defaults to whitespace on and identifiers off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\nSafe identifier shortening is AST/scope-aware and limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniforms/storage/texture/samplers, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, names are preserved. The minifier adds no dependency and does not produce compact-output source maps or struct-field renames yet.\n\nExisting import flattening and collision-avoidance mangling for imported shader graphs is unchanged. The loader calls the resolver with validation disabled; `resolveShader()` itself validates unminified output before diagnostics and validates the final output when identifiers are enabled.\n\nThe loader must run in asynchronous loader mode when imports are present. If async mode is unavailable, or if resolution fails, it reports a structured `VGPU-WGSL-*` error through webpack's callback. Package resolution is controlled by filesystem/package metadata rather than loader-specific syntax.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "ResolvedShader",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "ResolveOptions",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "resolveShader",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "SourceMap",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "WGSLAst",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl/runtime",
      "symbol": "WGSLModule",
      "repoPath": "packages/wgsl/src/runtime/resolveShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/runtime/resolveShader.docs.md",
      "content": "# resolveShader\n\n`resolveShader` loads a WGSL entry module, follows vgpu-wgsl import/export declarations, and returns a single plain WGSL string that can be passed to WebGPU. Callers can provide `modules` as an in-memory filesystem or let the resolver read files from disk; relative imports resolve from the importing module, `@/` imports use `rootDir`, and package imports use `packageMap` or package `exports`. With validation enabled, emitted WGSL is checked by the configured validator and failures are reported as structured `VGPU-WGSL-*` diagnostics.\n\n`ResolveOptions` contains the entry path plus optional `rootDir`, `packageMap`, `modules`, `validate`, and `minify` flags. `minify` accepts `boolean | { whitespace?: boolean; identifiers?: \"none\" | \"safe\" }` and defaults to `false`. `minify: true` is the production preset (`{ whitespace: true, identifiers: \"safe\" }`). Object form defaults to whitespace on and identifier renaming off, so `{ minify: { whitespace: true } }` is whitespace-only and `{ minify: { identifiers: \"safe\" } }` enables whitespace plus safe identifier shortening.\n\n`identifiers: \"safe\"` is AST/scope-aware and intentionally limited in this release to function-local `let`/`var`/`const`, function parameters, `for`-initializer locals, and resolver-generated private helper functions named like `_vgsl_<hash>__name` when analysis proves they are safe. It never renames entry points, resources/bindings/uniform/storage/texture/sampler declarations, overrides or references, struct/type names, struct fields, import/export/reflection-visible names, attributes, builtins, or WGSL predeclared names. If analysis is unsure, the conservative fallback preserves names. The minifier adds no dependency; compact-output source maps and struct-field renaming are deferred.\n\nWhen validation is enabled, `resolveShader()` validates the unminified emitted WGSL before minifying so diagnostics map to authored modules. If identifier minification is enabled, it also validates the final minified WGSL. Existing import flattening and collision-avoidance mangling is unchanged.\n\nThe output `ResolvedShader` contains `wgsl`, entry-point `cacheKey` values, `reflection`, a `sourceMap`, `diagnostics`, and `ast`. Resolution throws structured errors for missing files, invalid imports, missing exports, duplicate JavaScript-visible names, namespace misuse, mangle collisions, and validator errors.\n\n`WGSLAst` is a lightweight tooling view over the resolved graph. Its `modules` array contains `WGSLModule` records with module path, byte size, stable path hash prefix, imports, and exports; it does not expose parser internals. The AST also carries the same `cacheKey`, diagnostics, and `SourceMap` returned at the top level.\n\n`SourceMap` identifies the generated source list used for diagnostic remapping. It is line-oriented in this release: diagnostics expose authored file and line, while columns may be approximate when a line contains substituted identifiers. Approximate-column diagnostics keep their primary validator code and add `VGPU-WGSL-COL-APPROX` metadata so tooling can detect the limitation without parsing messages.\n"
    },
    {
      "package": "@vgpu/wgsl",
      "symbol": "compile",
      "repoPath": "packages/wgsl/src/compile.docs.md",
      "virtualPath": "/@vgpu/wgsl/compile.docs.md",
      "content": "# compile\n\n`compile(wgsl: string)` is the `@vgpu/wgsl` entry point. It accepts a plain WGSL\nruntime string and returns a `ResolvedShader` that `@vgpu/core` can turn into a\n`Shader` with `device.createShader(...)`.\n\nPublic behavior:\n\n- The input WGSL text is passed through byte-for-byte as `resolved.wgsl`.\n- Entry-point names are detected for convenience.\n- `source`, `sourceMap`, `ast`, `cacheKey`, and `stats` are populated with\n  passthrough metadata.\n- Any runtime `import` keyword throws a structured error with code\n  `VGPU-WGSL-RUNTIME-IMPORT`.\n\nInvariants: `compile()` performs no import resolution, package lookup, shader\nrewriting, or semantic validation beyond the explicit import rejection.\n\nExample:\n\n```ts\nconst resolved = compile(\"@compute @workgroup_size(1) fn main() {}\");\nconst shader = device.createShader(resolved);\n```\n"
    },
    {
      "package": "@vgpu/wgsl",
      "symbol": "ResolvedShader",
      "repoPath": "packages/wgsl/src/ResolvedShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/ResolvedShader.docs.md",
      "content": "# ResolvedShader\n\n`ResolvedShader` is the plain-WGSL shader description shared by `@vgpu/wgsl` and\n`@vgpu/core`. It is intentionally data-only: `compile(wgsl)` returns it, and\n`device.createShader(resolved)` turns it into a GPU shader module.\n\nPublic shape includes:\n\n- `kind: \"wgsl\"` and `wgsl`: the unchanged WGSL source.\n- `source`, `ast`, `sourceMap`: passthrough metadata with empty import and\n  diagnostics lists.\n- `cacheKey`: deterministic source key for future shader caches.\n- `entryPoints`: names detected from `@vertex`, `@fragment`, and `@compute`\n  declarations.\n- `stats`: line count, UTF-8 byte count, and placeholder bind-group count.\n\nInvariants: `compile()` does not resolve imports, mangle names, or build full\nreflection. Runtime strings containing `import` are rejected before a\n`ResolvedShader` is created. Consumers should treat fields as read-only and\npass the object across package boundaries rather than depending on the\nplaceholder AST internals.\n\nExample:\n\n```ts\nconst resolved = compile(wgslSource);\nconst shader = device.createShader(resolved);\n```\n"
    },
    {
      "package": "@vgpu/wgsl",
      "symbol": "SourceMap",
      "repoPath": "packages/wgsl/src/ResolvedShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/ResolvedShader.docs.md",
      "content": "# ResolvedShader\n\n`ResolvedShader` is the plain-WGSL shader description shared by `@vgpu/wgsl` and\n`@vgpu/core`. It is intentionally data-only: `compile(wgsl)` returns it, and\n`device.createShader(resolved)` turns it into a GPU shader module.\n\nPublic shape includes:\n\n- `kind: \"wgsl\"` and `wgsl`: the unchanged WGSL source.\n- `source`, `ast`, `sourceMap`: passthrough metadata with empty import and\n  diagnostics lists.\n- `cacheKey`: deterministic source key for future shader caches.\n- `entryPoints`: names detected from `@vertex`, `@fragment`, and `@compute`\n  declarations.\n- `stats`: line count, UTF-8 byte count, and placeholder bind-group count.\n\nInvariants: `compile()` does not resolve imports, mangle names, or build full\nreflection. Runtime strings containing `import` are rejected before a\n`ResolvedShader` is created. Consumers should treat fields as read-only and\npass the object across package boundaries rather than depending on the\nplaceholder AST internals.\n\nExample:\n\n```ts\nconst resolved = compile(wgslSource);\nconst shader = device.createShader(resolved);\n```\n"
    },
    {
      "package": "@vgpu/wgsl",
      "symbol": "WGSLAst",
      "repoPath": "packages/wgsl/src/ResolvedShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/ResolvedShader.docs.md",
      "content": "# ResolvedShader\n\n`ResolvedShader` is the plain-WGSL shader description shared by `@vgpu/wgsl` and\n`@vgpu/core`. It is intentionally data-only: `compile(wgsl)` returns it, and\n`device.createShader(resolved)` turns it into a GPU shader module.\n\nPublic shape includes:\n\n- `kind: \"wgsl\"` and `wgsl`: the unchanged WGSL source.\n- `source`, `ast`, `sourceMap`: passthrough metadata with empty import and\n  diagnostics lists.\n- `cacheKey`: deterministic source key for future shader caches.\n- `entryPoints`: names detected from `@vertex`, `@fragment`, and `@compute`\n  declarations.\n- `stats`: line count, UTF-8 byte count, and placeholder bind-group count.\n\nInvariants: `compile()` does not resolve imports, mangle names, or build full\nreflection. Runtime strings containing `import` are rejected before a\n`ResolvedShader` is created. Consumers should treat fields as read-only and\npass the object across package boundaries rather than depending on the\nplaceholder AST internals.\n\nExample:\n\n```ts\nconst resolved = compile(wgslSource);\nconst shader = device.createShader(resolved);\n```\n"
    },
    {
      "package": "@vgpu/wgsl",
      "symbol": "WGSLSource",
      "repoPath": "packages/wgsl/src/ResolvedShader.docs.md",
      "virtualPath": "/@vgpu/wgsl/ResolvedShader.docs.md",
      "content": "# ResolvedShader\n\n`ResolvedShader` is the plain-WGSL shader description shared by `@vgpu/wgsl` and\n`@vgpu/core`. It is intentionally data-only: `compile(wgsl)` returns it, and\n`device.createShader(resolved)` turns it into a GPU shader module.\n\nPublic shape includes:\n\n- `kind: \"wgsl\"` and `wgsl`: the unchanged WGSL source.\n- `source`, `ast`, `sourceMap`: passthrough metadata with empty import and\n  diagnostics lists.\n- `cacheKey`: deterministic source key for future shader caches.\n- `entryPoints`: names detected from `@vertex`, `@fragment`, and `@compute`\n  declarations.\n- `stats`: line count, UTF-8 byte count, and placeholder bind-group count.\n\nInvariants: `compile()` does not resolve imports, mangle names, or build full\nreflection. Runtime strings containing `import` are rejected before a\n`ResolvedShader` is created. Consumers should treat fields as read-only and\npass the object across package boundaries rather than depending on the\nplaceholder AST internals.\n\nExample:\n\n```ts\nconst resolved = compile(wgslSource);\nconst shader = device.createShader(resolved);\n```\n"
    }
  ]
}
;